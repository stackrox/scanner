package imagescan

// This file does not define any unit tests so far. However, its presence ensures that
// unit tests are run for this package, which among other things ensures that the
// initialization of global variables (which might panic) is performed.