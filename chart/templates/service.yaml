apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}-db
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: https
    port: 5432
    targetPort: postgresql
  selector:
    app: {{ .Values.appName }}-db
  type: ClusterIP
